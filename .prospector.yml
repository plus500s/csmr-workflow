output-format: grouped

ignore-paths:
  - build
  - migrations
  - tests

pylint:
  disable:
    - too-many-ancestors
    - missing-docstring
  options:
    max-line-length: 120

pep8:
  full: true

mccabe:
  run: false
